name: my first git hub actions

on:
push:
branches: main

jobs:
build:
runs-on: ubuntu-latest

steps:
- uses: actions/checkout@v4
- name: my fisrt step
  run:
      echo "This is fisrt repo"
      uname -a
